"use strict";

const scrap = require( "./scrap" );
const updateStatus = require( "./updateStatus" );
const clearCache = require( "./clearCache" );

module.exports = {
  scrap,
  updateStatus,
  clearCache
};
